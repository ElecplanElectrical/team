function safeInternalUrl(value){return typeof value==="string"&&value.startsWith("/")&&!value.startsWith("//")?value:"/team-chat"}
self.addEventListener("push",event=>{let data={};try{data=event.data?event.data.json():{}}catch{}event.waitUntil(self.registration.showNotification(data.title||"YourPlan Team",{body:data.body||"New team message",icon:"/elecplan-app-icon.svg",badge:"/elecplan-app-icon.svg",tag:data.tag||"yourplan-chat",data:{url:safeInternalUrl(data.url)}}))});
self.addEventListener("notificationclick",event=>{event.notification.close();const url=safeInternalUrl(event.notification.data?.url);event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{for(const client of list){if("focus" in client){client.navigate(url);return client.focus()}}return clients.openWindow(url)}))});
