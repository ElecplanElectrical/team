import Link from "next/link";
import { notFound } from "next/navigation";
import { ChevronLeft, Download, FileText, FolderOpen } from "lucide-react";
import { getPlatformAdmin } from "@/lib/platform-admin";
const g="#69be28",d="#080a0b",p="#111416",b="#2a3033",m="#9aa3a7";
const docs=[['Race Weekend','Round 1 run sheet','PDF','Updated today'],['Race Weekend','Track map & paddock info','PDF','Event file'],['Team','Team procedures','PDF','Current'],['Riders','#100 setup export','PDF','Practice'],['Riders','#111 setup export','PDF','Qualifying'],['Riders','#63 setup export','PDF','Practice'],['Media','Sponsor & media guide','PDF','Season'],['Admin','Travel / event information','PDF','Round 1']];
export default async function Documents(){const user=await getPlatformAdmin();if(!user)notFound();return <main className="min-h-screen text-white" style={{background:d}}><header className="border-b" style={{borderColor:b}}><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3 md:px-6"><Link href="/hq/demos/empire" className="flex items-center gap-1 text-xs" style={{color:m}}><ChevronLeft size={15}/>Empire HQ</Link><span className="text-[10px] font-bold uppercase tracking-[.25em]" style={{color:g}}>Documents · Demo</span></div></header><div className="mx-auto max-w-6xl p-4 md:p-6"><section className="rounded-2xl border p-6 md:p-8" style={{background:"linear-gradient(125deg,#111416,#0b0d0e 70%,#15220e)",borderColor:b}}><div className="flex items-center gap-3"><FolderOpen style={{color:g}}/><div><p className="text-xs font-bold uppercase tracking-[.25em]" style={{color:g}}>Team files</p><h1 className="mt-1 text-3xl font-black uppercase md:text-4xl">Documents</h1></div></div><p className="mt-4 text-sm" style={{color:m}}>Event files, team procedures, setup exports and shared resources with role-based access.</p></section><section className="mt-5 overflow-hidden rounded-xl border" style={{background:p,borderColor:b}}>{docs.map(([folder,name,type,updated])=><div key={folder+name} className="grid gap-3 border-b p-4 md:grid-cols-[180px_1fr_80px_140px_30px] md:items-center" style={{borderColor:b}}><span className="text-xs font-bold uppercase tracking-wider" style={{color:g}}>{folder}</span><span className="flex items-center gap-2 text-sm font-semibold"><FileText size={15} style={{color:m}}/>{name}</span><span className="text-xs" style={{color:m}}>{type}</span><span className="text-xs" style={{color:m}}>{updated}</span><Download size={15} style={{color:g}}/></div>)}</section></div></main>}
