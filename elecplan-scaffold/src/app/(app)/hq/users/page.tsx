import Link from"next/link";import{notFound}from"next/navigation";import{prisma}from"@/lib/prisma";import{getPlatformAdmin}from"@/lib/platform-admin";import TopBar from"@/components/TopBar";export default async function UsersPage(){const admin=await getPlatformAdmin();if(!admin)notFound();const rows=await prisma.businessPortal.findMany({orderBy:{name:"asc"},include:{_count:{select:{users:true}}}});return <><TopBar title="Users & Access" subtitle="Portal users grouped by customer"/><main className="flex-1 overflow-auto bg-[#03101f] p-4 md:p-6"><div className="mx-auto max-w-6xl grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{rows.map(p=><Link key={p.id} href={`/hq/portals/${p.slug}#users`} className="rounded-xl border border-white/10 bg-[#07192b] p-5 hover:border-[#168dff]/45"><p className="font-semibold text-white">{p.name}</p><p className="mt-2 text-2xl font-bold text-[#25c7ff]">{p._count.users}</p><p className="text-xs text-slate-500">portal users</p></Link>)}</div></main></>}
