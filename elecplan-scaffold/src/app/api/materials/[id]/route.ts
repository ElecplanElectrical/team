import { NextResponse } from "next/server";
import { z } from "zod";
import { prisma } from "@/lib/prisma";
import { getSessionUser } from "@/lib/session";
import { recordAudit } from "@/lib/audit";
const schema=z.union([z.object({delta:z.number().int().min(-1000).max(1000).refine(v=>v!==0)}),z.object({onHand:z.number().int().min(0)}),z.object({name:z.string().min(2).max(160),supplier:z.string().max(100).nullable().optional()})]);
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const user=await getSessionUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid update"},{status:400});const{id}=await params;if("name" in parsed.data){try{return NextResponse.json(await prisma.stockItem.update({where:{id},data:{name:parsed.data.name,supplier:parsed.data.supplier??undefined}}))}catch{return NextResponse.json({error:"Stock item not found"},{status:404})}}if("delta" in parsed.data){const delta=parsed.data.delta;const result=await prisma.stockItem.updateMany({where:{id,...(delta<0?{onHand:{gte:Math.abs(delta)}}:{})},data:{onHand:delta>0?{increment:delta}:{decrement:Math.abs(delta)}}});if(!result.count)return NextResponse.json({error:"Stock quantity changed. Refresh and try again."},{status:409});const item=await prisma.stockItem.findUnique({where:{id}});if(!item)return NextResponse.json({error:"Stock item not found"},{status:404});await recordAudit({actor:user,action:"STOCK_QUANTITY_CHANGED",entityType:"StockItem",entityId:item.id,details:{name:item.name,before:item.onHand-delta,after:item.onHand,delta,mode:"delta"}});return NextResponse.json(item)}const before=await prisma.stockItem.findUnique({where:{id},select:{id:true,name:true,onHand:true}});if(!before)return NextResponse.json({error:"Stock item not found"},{status:404});try{const item=await prisma.stockItem.update({where:{id},data:{onHand:parsed.data.onHand}});if(before.onHand!==item.onHand)await recordAudit({actor:user,action:"STOCK_QUANTITY_CHANGED",entityType:"StockItem",entityId:item.id,details:{name:item.name,before:before.onHand,after:item.onHand,delta:item.onHand-before.onHand,mode:"absolute"}});return NextResponse.json(item)}catch{return NextResponse.json({error:"Stock item not found"},{status:404})}}
