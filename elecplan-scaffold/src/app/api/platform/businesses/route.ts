import { NextResponse } from "next/server";
import { z } from "zod";
import { prisma } from "@/lib/prisma";
import { getPlatformAdmin } from "@/lib/platform-admin";
import { DEFAULT_MODULES } from "@/lib/brand";
import { generateToken, expiryFromNow, setPasswordUrl, INVITE_TTL_HOURS } from "@/lib/tokens";

const schema=z.object({name:z.string().trim().min(2).max(120),industry:z.string().trim().max(120).optional().default(""),contactName:z.string().trim().min(1).max(120),contactEmail:z.string().trim().toLowerCase().email().max(160),monthlyPrice:z.union([z.string(),z.number()]).optional(),modules:z.array(z.enum(DEFAULT_MODULES)).min(1),logoUrl:z.string().trim().url().optional().or(z.literal("")),primaryColor:z.string().trim().regex(/^#[0-9a-fA-F]{6}$/).optional().default("#168dff"),accentColor:z.string().trim().regex(/^#[0-9a-fA-F]{6}$/).optional().default("#25c7ff"),plan:z.string().trim().min(1).max(80).optional().default("CUSTOM"),setupFee:z.union([z.string(),z.number()]).optional(),gracePeriodDays:z.number().int().min(0).max(90).optional().default(7)});
function slugify(value:string){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,60)}
async function requirePlatformAdmin(){return getPlatformAdmin()}
export async function GET(){const admin=await requirePlatformAdmin();if(!admin)return NextResponse.json({error:"Platform owner access required"},{status:403});const businesses=await prisma.businessPortal.findMany({select:{id:true,name:true,slug:true,industry:true,contactName:true,contactEmail:true,logoUrl:true,primaryColor:true,accentColor:true,modules:true,plan:true,monthlyPrice:true,active:true,createdAt:true,updatedAt:true,_count:{select:{users:true,clients:true,jobs:true}}},orderBy:{createdAt:"desc"}});return NextResponse.json(businesses)}
export async function POST(req:Request){const admin=await requirePlatformAdmin();if(!admin)return NextResponse.json({error:"Platform owner access required"},{status:403});const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Check the business details, owner email, branding and modules."},{status:400});let slug=slugify(parsed.data.name);if(!slug)return NextResponse.json({error:"Business name cannot create a portal address."},{status:400});const existingSlug=await prisma.businessPortal.findUnique({where:{slug},select:{id:true}});if(existingSlug)slug=`${slug}-${Math.random().toString(36).slice(2,6)}`;const existingUser=await prisma.user.findUnique({where:{email:parsed.data.contactEmail},select:{id:true}});if(existingUser)return NextResponse.json({error:"That owner email already belongs to a YourPlan user."},{status:409});const rawPrice=parsed.data.monthlyPrice;const monthlyPrice=rawPrice==null||String(rawPrice).trim()===""?null:Number(rawPrice);if(monthlyPrice!==null&&(!Number.isFinite(monthlyPrice)||monthlyPrice<0))return NextResponse.json({error:"Enter a valid monthly subscription."},{status:400});const rawSetup=parsed.data.setupFee;const setupFee=rawSetup==null||String(rawSetup).trim()===""?0:Number(rawSetup);if(!Number.isFinite(setupFee)||setupFee<0)return NextResponse.json({error:"Enter a valid setup fee."},{status:400});const{raw,hash}=generateToken();try{const result=await prisma.$transaction(async tx=>{const business=await tx.businessPortal.create({data:{name:parsed.data.name,slug,industry:parsed.data.industry||null,contactName:parsed.data.contactName,contactEmail:parsed.data.contactEmail,logoUrl:parsed.data.logoUrl||null,primaryColor:parsed.data.primaryColor,accentColor:parsed.data.accentColor,modules:parsed.data.modules,plan:parsed.data.plan,monthlyPrice}});await tx.$executeRaw`INSERT INTO "BusinessSubscription" ("businessId","status","setupFee","gracePeriodDays") VALUES (${business.id}, 'ACTIVE', ${setupFee}, ${parsed.data.gracePeriodDays}) ON CONFLICT ("businessId") DO NOTHING`;const owner=await tx.user.create({data:{businessId:business.id,name:parsed.data.contactName,email:parsed.data.contactEmail,role:"ADMIN",passwordTokens:{create:{tokenHash:hash,type:"INVITE",expiresAt:expiryFromNow(INVITE_TTL_HOURS)}}},select:{id:true,name:true,email:true,role:true}});await tx.auditLog.create({data:{actorId:admin.id,actorEmail:admin.email,action:"PLATFORM_CUSTOMER_CREATED",entityType:"BusinessPortal",entityId:business.id,details:{businessName:business.name,plan:business.plan,monthlyPrice:monthlyPrice===null?null:String(monthlyPrice),setupFee:String(setupFee),gracePeriodDays:parsed.data.gracePeriodDays,ownerEmail:owner.email}}});return{business,owner}});const inviteUrl=setPasswordUrl(new URL(req.url).origin,raw);return NextResponse.json({...result,inviteUrl,portalPath:`/b/${result.business.slug}/dashboard`},{status:201})}catch{return NextResponse.json({error:"Could not create the customer portal and owner account."},{status:400})}}
