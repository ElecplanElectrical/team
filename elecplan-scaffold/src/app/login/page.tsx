import type { Metadata, Viewport } from "next";import Link from "next/link";import { headers } from "next/headers";import { Layers3, ShieldCheck, Sparkles } from "lucide-react";import LoginForm from "@/components/LoginForm";import YourPlanLogo from "@/components/YourPlanLogo";import TenantLoginForm from "@/components/TenantLoginForm";import { prisma } from "@/lib/prisma";
function safeCallback(value?:string){if(!value||!value.startsWith("/")||value.startsWith("//"))return"/";try{const url=new URL(value,"https://your-plan.com.au");return url.origin==="https://your-plan.com.au"?`${url.pathname}${url.search}${url.hash}`:"/"}catch{return"/"}}function tenantSlugFromCallback(value:string){return value.match(/^\/b\/([a-z0-9-]+)\/dashboard(?:[/?#]|$)/i)?.[1]?.toLowerCase()??null}
export async function generateMetadata():Promise<Metadata>{const h=await headers(),host=(h.get("x-forwarded-host")??h.get("host")??"").split(":")[0].toLowerCase();if(host==="qls.your-plan.com.au")return{title:{absolute:"Sign in | Quality Landscape Solutions"},description:"Private Quality Landscape Solutions team portal.",robots:{index:false,follow:false}};return{title:"Sign in"}}export async function generateViewport():Promise<Viewport>{return{themeColor:"#03070b"}}
export default async function LoginPage({searchParams}:{searchParams:Promise<{callbackUrl?:string;tenant?:string}>}){const{callbackUrl,tenant}=await searchParams,destination=safeCallback(callbackUrl),requestedTenant=/^[a-z0-9-]+$/.test(tenant??"")?tenant!.toLowerCase():null,tenantSlug=requestedTenant??tenantSlugFromCallback(destination);if(tenantSlug){const business=await prisma.businessPortal.findFirst({where:{slug:tenantSlug,active:true},select:{name:true,slug:true,logoUrl:true,primaryColor:true,accentColor:true,brandingConfig:true}});if(business)return <TenantLoginForm callbackUrl={destination} tenantSlug={business.slug} businessName={business.name} shortName={business.slug.toUpperCase()} logoUrl={business.logoUrl} primaryColor={business.primaryColor} accentColor={business.accentColor} brandingConfig={business.brandingConfig}/>}
return <main className="relative min-h-screen overflow-hidden bg-[#03070b] text-white [font-family:Inter,Arial,sans-serif]"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_12%_14%,rgba(22,141,255,.24),transparent_28%),radial-gradient(circle_at_88%_82%,rgba(37,199,255,.12),transparent_25%),linear-gradient(135deg,#060d14_0%,#03070b_55%,#07111b_100%)]"/><div className="relative z-10 mx-auto grid min-h-screen w-[92%] max-w-[1380px] lg:grid-cols-[1.08fr_.92fr]"><section className="flex min-h-[310px] flex-col justify-between py-7 lg:min-h-screen lg:py-9 lg:pr-16"><header className="flex items-center justify-between"><Link href="/" className="inline-flex"><YourPlanLogo width={205}/></Link></header><div className="max-w-[670px] py-12 lg:py-16"><div className="inline-flex items-center gap-2 rounded-full border border-[#168dff]/25 bg-[#168dff]/[.07] px-3 py-2 text-[11px] font-semibold uppercase tracking-[.14em] text-[#57b7ff]"><Sparkles size={14}/> Your business operating system</div><h1 className="mt-7 text-[42px] font-black leading-[.98] tracking-[-.055em] sm:text-[56px] lg:text-[70px]">One sign-in.<br/><span className="text-[#168dff]">Your whole business.</span></h1><p className="mt-6 max-w-[570px] text-[15px] leading-7 text-slate-300 sm:text-[17px]">Jobs, scheduling, customers, staff, documents, billing and reporting—connected in one secure place.</p><div className="mt-8 grid max-w-[620px] gap-3 sm:grid-cols-3"><Info icon={<Layers3 size={19}/>} title="One platform"/><Info icon={<ShieldCheck size={19}/>} title="Secure access"/><Info icon={<Sparkles size={19}/>} title="Built your way"/></div></div></section><section className="flex items-center justify-center border-t border-white/[.07] py-10 lg:min-h-screen lg:border-l lg:border-t-0 lg:border-white/[.07] lg:pl-16"><LoginForm callbackUrl={destination}/></section></div></main>}
function Info({icon,title}:{icon:React.ReactNode;title:string}){return <div className="rounded-xl border border-white/10 bg-white/[.025] p-4 text-[#168dff]">{icon}<strong className="mt-3 block text-[13px] text-white">{title}</strong></div>}
