"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Plus } from "lucide-react"; import { DEFAULT_MODULES } from "@/lib/brand";
const UI={panel:"#07192b",alt:"#041323",border:"rgba(77,150,221,.24)",text:"#f5f9ff",mute:"#93a9c2",cyan:"#25c7ff",blue:"#168dff"};
const LABEL:Record<string,string>={dashboard:"Dashboard",jobs:"Jobs",calendar:"Calendar",clients:"Clients",leads:"Leads",quotes:"Quotes",invoices:"Invoices",employees:"Employees",timesheets:"Timesheets",inspections:"Inspections",documents:"Documents",materials:"Materials",reminders:"Reminders",analytics:"Reports"};
export default function BusinessPortalForm(){const router=useRouter();const[name,setName]=useState("");const[industry,setIndustry]=useState("");const[price,setPrice]=useState("");const[modules,setModules]=useState<string[]>([...DEFAULT_MODULES]);const[saving,setSaving]=useState(false);const[error,setError]=useState("");function toggle(m:string){setModules(x=>x.includes(m)?x.filter(v=>v!==m):[...x,m])}async function submit(){setError("");if(!name.trim()){setError("Business name is required.");return}setSaving(true);const r=await fetch("/api/platform/businesses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,industry,monthlyPrice:price,modules})});const b=await r.json().catch(()=>null);setSaving(false);if(!r.ok){setError(b?.error||"Could not create business portal.");return}setName("");setIndustry("");setPrice("");router.refresh()}return <section className="rounded-xl p-5" style={{background:UI.panel,border:`1px solid ${UI.border}`}}><div className="flex items-center gap-2"><Plus size={18} style={{color:UI.cyan}}/><div><h2 className="text-sm font-semibold" style={{color:UI.text}}>Create customer business</h2><p className="mt-1 text-xs" style={{color:UI.mute}}>Set the business identity, subscription and modules. Branding can be refined after creation.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-3"><Field label="Business name" value={name} set={setName} placeholder="Quality Landscape Solutions"/><Field label="Industry" value={industry} set={setIndustry} placeholder="Landscaping"/><Field label="Monthly subscription (AUD)" value={price} set={setPrice} placeholder="299" type="number"/></div><p className="mt-5 text-xs font-semibold" style={{color:UI.mute}}>Enabled modules</p><div className="mt-2 flex flex-wrap gap-2">{DEFAULT_MODULES.map(m=><button type="button" key={m} onClick={()=>toggle(m)} className="rounded-full px-3 py-1.5 text-xs font-medium" style={{background:modules.includes(m)?"rgba(22,141,255,.18)":UI.alt,border:`1px solid ${modules.includes(m)?UI.blue:UI.border}`,color:modules.includes(m)?"#fff":UI.mute}}>{LABEL[m]||m}</button>)}</div>{error&&<p className="mt-4 text-xs text-red-400">{error}</p>}<div className="mt-5 flex justify-end"><button type="button" disabled={saving} onClick={submit} className="rounded-lg px-4 py-2.5 text-sm font-semibold text-white" style={{background:UI.blue}}>{saving?"Creating…":"Create business portal"}</button></div></section>}
function Field({label,value,set,placeholder,type="text"}:{label:string;value:string;set:(v:string)=>void;placeholder:string;type?:string}){return <label className="text-xs" style={{color:UI.mute}}>{label}<input type={type} value={value} onChange={e=>set(e.target.value)} placeholder={placeholder} className="mt-1 h-11 w-full rounded-lg px-3 text-sm outline-none" style={{background:UI.alt,border:`1px solid ${UI.border}`,color:UI.text}}/></label>}
