"use client";
import type{ReactNode}from"react";import{usePathname}from"next/navigation";import EmpireDemoNav from"@/components/EmpireDemoNav";export default function EmpireDemoShell({children}:{children:ReactNode}){const path=usePathname();const login=path==="/hq/demos/empire/login";const presentation=path==="/hq/demos/empire/present";if(login)return <div className="min-h-screen bg-[#080a0b] text-white">{children}</div>;return <div className="min-h-screen bg-[#0b0d0e] text-white"><EmpireDemoNav/><div className={`lg:pl-[244px] ${presentation?'':'pb-24 md:pb-0'}`}>{children}</div></div>}
