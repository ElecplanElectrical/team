"use client";
import {useState}from"react";import Image from"next/image";import Link from"next/link";import{usePathname}from"next/navigation";import{signOut}from"next-auth/react";import{Bell,Building2,CircleDollarSign,CreditCard,FileText,LayoutGrid,LogOut,Menu,Receipt,UserPlus,Users,X}from"lucide-react";import type{Role}from"@prisma/client";import type{YourPlanModule}from"@/lib/brand";import{SCREEN_PATH}from"@/lib/access";import{navGroupsFor}from"@/lib/nav";import YourPlanLogo from"@/components/YourPlanLogo";import QlsBrandLockup from"@/components/QlsBrandLockup";
const UI={mute:"#9aaeaa",faint:"var(--brand-faint, #647a6d)"};type Brand={name:string;slug:string;logoUrl:string|null;primaryColor:string;accentColor:string;modules:YourPlanModule[]}|null;type HqItem={label:string;href:string;icon:typeof LayoutGrid};const HQ_GROUPS:{heading?:string;items:HqItem[]}[]=[{items:[{label:"Dashboard",href:"/hq",icon:LayoutGrid},{label:"Customers",href:"/hq#customers",icon:Building2},{label:"Leads",href:"/leads",icon:Users}]},{heading:"Sales & Billing",items:[{label:"Quotes",href:"/quotes",icon:FileText},{label:"Invoices",href:"/invoices",icon:Receipt},{label:"Payments",href:"/hq#payments",icon:CircleDollarSign},{label:"Subscriptions",href:"/hq#customers",icon:CreditCard}]},{heading:"Customer Management",items:[{label:"Portals",href:"/hq#customers",icon:Building2},{label:"Onboarding",href:"/hq#onboarding",icon:UserPlus},{label:"Users",href:"/hq#customers",icon:Users}]}];
function MobileBrand({brand,drawer=false}:{brand?:Brand;drawer?:boolean}){if(brand?.slug==="qls")return <QlsBrandLockup variant={drawer?"drawer":"mobile"}/>;if(!brand?.logoUrl)return <YourPlanLogo width={drawer?144:112}/>;return <div className="flex items-center gap-2.5"><Image unoptimized src={brand.logoUrl} alt={brand.name} width={300} height={220} className={drawer?"h-28 w-auto max-w-[150px] object-contain object-left":"h-11 w-11 object-contain"}/></div>}
export default function MobileNav({role,brand,platformAdmin=false}:{role:Role;brand?:Brand;platformAdmin?:boolean}){const pathname=usePathname();const[open,setOpen]=useState(false);const isQls=brand?.slug==="qls";const groups=navGroupsFor(role,brand?.modules).map(group=>({...group,items:isQls?group.items.filter(item=>item.screen!=="inspections"):group.items})).filter(group=>group.items.length>0);const primary=isQls?"#50d878":brand?.primaryColor||"#168dff";const accent=isQls?"#82eca0":brand?.accentColor||"#25c7ff";const border=isQls?"rgba(130,236,160,.20)":brand?`${accent}36`:"rgba(73,145,214,.22)";const panel=isQls?"#111613":brand?"#10261a":"var(--brand-panel, #07192b)";const label=platformAdmin?"YourPlan HQ":brand?.name||"Your Plan";const logoutUrl=isQls?"/login?tenant=qls&callbackUrl=%2Fdashboard":"/login";const homeHref=platformAdmin?"/hq":"/dashboard";return <><div className="fixed inset-x-0 top-0 z-50 flex h-16 items-center justify-between px-4 md:hidden" style={{background:isQls?"rgba(3,6,5,.98)":"rgba(2,14,27,.97)",borderBottom:`1px solid ${border}`,backdropFilter:"blur(16px)"}}><button type="button" onClick={()=>setOpen(true)} aria-label="Open navigation" className="flex h-10 w-10 items-center justify-center rounded-lg" style={{background:panel,border:`1px solid ${border}`,color:UI.mute}}><Menu size={19}/></button><Link href={homeHref} aria-label={`${label} dashboard`} className="flex h-14 min-w-0 max-w-[230px] items-center justify-center"><MobileBrand brand={brand}/></Link><Link href="/notifications" aria-label="Open notifications" className="flex h-10 w-10 items-center justify-center rounded-lg" style={{background:panel,border:`1px solid ${border}`,color:UI.mute}}><Bell size={17}/></Link></div>{open&&<div className="fixed inset-0 z-[60] md:hidden"><button type="button" aria-label="Close navigation overlay" className="absolute inset-0 bg-black/70 backdrop-blur-sm" onClick={()=>setOpen(false)}/><aside className="absolute inset-y-0 left-0 flex w-[84%] max-w-[340px] flex-col overflow-hidden p-4 shadow-2xl" style={{background:isQls?"#151917":`linear-gradient(180deg,#02101f,#031321)`,borderRight:`1px solid ${border}`}}>{isQls&&<div className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true"><Image src="/qls-tree-portal.webp" alt="" width={1491} height={1055} className="absolute left-[47%] top-[170px] h-[720px] w-auto max-w-none -translate-x-1/2 grayscale opacity-[0.055] brightness-125 contrast-75"/></div>}<div className="relative z-10 mb-5 flex items-center justify-between"><MobileBrand brand={brand} drawer/><button type="button" onClick={()=>setOpen(false)} className="flex h-10 w-10 items-center justify-center rounded-lg" style={{background:panel,border:`1px solid ${border}`,color:UI.mute}}><X size={18}/></button></div><nav className="relative z-10 min-h-0 flex-1 space-y-5 overflow-y-auto pr-1">{platformAdmin?HQ_GROUPS.map((group,gi)=><div key={group.heading??gi}>{group.heading&&<p className="mb-2 px-2 text-[10px] font-semibold uppercase tracking-[0.13em]" style={{color:UI.faint}}>{group.heading}</p>}<div className="space-y-1">{group.items.map(item=>{const active=pathname===item.href;const Icon=item.icon;return <Link key={item.label} href={item.href} onClick={()=>setOpen(false)} className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium" style={{background:active?`linear-gradient(90deg,${primary}b8,${primary}35)`:"transparent",color:active?"#f4f8ff":UI.mute}}><Icon size={17} style={{color:active?accent:undefined}}/>{item.label}</Link>})}</div></div>):groups.map((group,gi)=><div key={group.heading??gi}>{group.heading&&<p className="mb-2 px-2 text-[10px] font-semibold uppercase tracking-[0.13em]" style={{color:UI.faint}}>{group.heading}</p>}<div className="space-y-1">{group.items.map(item=>{const href=SCREEN_PATH[item.screen];const active=pathname===href||pathname.startsWith(href+"/");const Icon=item.icon;return <Link key={item.screen} href={href} onClick={()=>setOpen(false)} className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium" style={{background:active?`linear-gradient(90deg,${primary}b8,${primary}35)`:"transparent",color:active?"#f4f8ff":UI.mute}}><Icon size={17} style={{color:active?accent:undefined}}/>{item.label}</Link>})}</div></div>)}</nav><button type="button" onClick={()=>signOut({callbackUrl:logoutUrl})} className="relative z-10 mt-4 flex items-center gap-3 border-t px-3 pt-5 text-sm font-medium" style={{borderColor:border,color:UI.mute}}><LogOut size={17}/> Sign out{isQls?" of QLS":""}</button></aside></div>}</>}
